# GIF 与 MPEG-4 媒体支持检查

检查日期：2026-07-21

## 结论

当前网站完整支持 JPG、PNG 等静态图片，但尚未完整支持 GIF 动图和 MPEG-4（MP4）视频。

## 1. 首页作品卡片

- 卡片封面使用 Three.js `TextureLoader` 加载。
- JPG、PNG 等静态图片可正常显示。
- GIF 文件可能被加载，但 WebGL 纹理通常只保留静态帧，不能作为可靠的动态 GIF 播放方案。
- MP4 不能被 `TextureLoader` 当作普通图片纹理播放。
- 当前首页没有 `VideoTexture`、视频元素或 GIF 逐帧刷新逻辑。

结论：不支持动态 GIF 和 MP4 播放。

## 2. 项目详情页

- 项目媒体数组统一通过 `<img>` 渲染。
- GIF 在浏览器支持的情况下可以通过 `<img>` 播放。
- MP4 被传入 `<img src>` 后无法播放，也不会显示视频控件。
- 当前数据结构没有记录媒体类型，也没有根据扩展名切换 `<img>` 与 `<video>`。

结论：GIF 可播放；MP4 不支持。

## 3. CMS 导入、保存与预览

- 文件选择器使用 `accept="image/*"`，一般允许 GIF，但不允许选择 MP4。
- 服务端目前没有 MIME 类型白名单，会直接保存收到的文件；这意味着绕过前端后 MP4 可能被写入，但并不代表页面能播放。
- CMS 预览统一使用 `<img>`，GIF 可播放，MP4 无法预览。
- 媒体路径会保存到 `images` 数组，没有 `type`、封面帧、时长等视频字段。
- “设为封面”未限制媒体类型；若将视频路径设为封面，首页会加载失败。

结论：GIF 的导入、保存和预览基本可用；MP4 的正常导入与预览不支持。

## 建议改造

1. 将文件选择器改为 `accept="image/*,video/mp4"`。
2. 服务端只允许明确的图片类型、GIF 和 `video/mp4`，并设置文件大小限制。
3. 将媒体数据由字符串改为 `{ src, type, poster }`。
4. CMS 和详情页根据 `type` 分别渲染 `<img>` 或 `<video>`。
5. MP4 使用单独的静态封面图作为首页 Three.js 纹理；详情页再播放视频。
6. GIF 若需要在首页动态播放，应转为 MP4/WebM 并使用 Three.js `VideoTexture`，性能与兼容性更稳定。

## 2026-07-21 实施状态

- 已支持 CMS 导入 JPG、PNG、WebP、GIF 和 MP4。
- 已增加服务端 MIME/扩展名白名单与单文件 100MB 限制。
- 已支持 CMS 中预览 GIF 和带控制栏的 MP4。
- 已支持项目详情页按类型显示图片或 MP4 视频；视频不自动播放。
- 已限制首页封面只能使用 JPG、PNG 或 WebP 静态图片。
- 已保持原有字符串媒体路径兼容，无需迁移现有项目数据。
