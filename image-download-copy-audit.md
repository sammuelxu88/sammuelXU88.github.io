# 图片下载与复制检查

检查日期：2026-07-21

## 结论

当前网站中的作品图片可以被下载或复制，网站没有设置专门的图片保护措施。

## 首页作品墙

- 首页图片绘制在 Three.js WebGL 画布中，用户通常不能直接对单张卡片使用“图片另存为”。
- 但图片原始地址会由浏览器请求，访客仍可通过开发者工具、网络记录或项目数据找到并下载原图。
- 画布内容也可通过截图或画布捕获保存。

## 项目详情页

- 详情页使用普通 `<img>` 元素展示作品。
- 未禁用右键菜单，支持“图片另存为”和复制图片。
- 未设置 `draggable=false`，默认可以拖拽图片到桌面或其他应用。
- 图片可以在新标签页打开并下载。

## CMS

- CMS 缩略图和预览图同样使用普通 `<img>` 元素。
- 可以右键保存、复制和拖拽。
- CMS 依赖密码访问，但图片文件本身位于公开目录，知道地址后不需要 CMS 密码即可访问。

## 服务端检查

- 静态作品图请求返回 HTTP 200 和正常的 `image/jpeg` 类型。
- 未设置禁止下载的 `Content-Disposition`。
- 未设置防盗链、来源校验或访问授权。
- 示例图片地址可直接访问：`/portfolio/thumbs/back-to-school-sale.jpg`。

## 风险说明

网页无法真正阻止用户保存已经显示到浏览器中的图片。禁用右键、禁止拖拽或覆盖透明层只能减少普通操作，不能阻止截图、开发者工具和网络请求下载。

## 建议

1. 公开页面只提供压缩后的展示图，不公开原始高分辨率文件。
2. 展示图可增加低透明署名水印。
3. 详情页可禁用图片右键和拖拽，作为轻量操作限制。
4. 原始文件放在非公开目录，仅通过需要授权的下载接口提供。
5. CMS 上传后自动生成展示尺寸副本，首页和详情页只使用副本。
