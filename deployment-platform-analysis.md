# 作品集部署平台分析

## 当前项目特征

- Next.js 16 App Router 项目。
- 首页包含 Three.js / WebGL 作品墙和较多图片、GIF、MP4 等媒体资源。
- 项目详情页在构建时生成静态页面。
- CMS 使用 `/api/cms/projects` 服务端接口。
- CMS 会直接写入 `content/projects.json` 与 `public/portfolio/...`，要求持久化文件系统。

## 推荐结论

### 首选：腾讯云 CloudBase Run / 云托管

适合希望中国大陆访问更稳定，并保留 CMS、服务端接口和后续扩展能力的正式版本。应使用云托管或容器运行完整 Next.js 服务，不应只使用静态网站托管。CMS 数据建议进一步迁移到 CloudBase 数据库，图片和视频迁移到云存储，以获得可靠的永久保存能力。

### 次选：Vercel

最容易部署当前 Next.js 项目，前台页面、动态接口和自动构建支持最好，适合快速上线和海外招聘访问。但 Vercel Functions 的运行文件系统不适合作为永久 CMS 存储；上线前需将项目数据迁移到数据库，将图片和视频迁移到对象存储。迁移完成后，Vercel 是开发与维护成本最低的方案。

### 可选：Netlify

能够运行 Next.js 和函数，但同样不能依赖函数运行目录永久写入 JSON 与媒体文件。需要数据库和对象存储改造。对本项目而言没有明显优于 Vercel 的优势。

### 不推荐：GitHub Pages

GitHub Pages 只托管静态 HTML、CSS 和 JavaScript，不能运行当前 `/api/cms/projects` 服务端接口，也不能让 CMS 永久写入文件。只有移除在线 CMS、改成静态导出并通过 Git 提交内容后才适用。

### 不可选：Gitee Pages

Gitee 官方帮助中心已标注 Pages、Pages Pro 与单页面应用支持均已下线。

### 有条件可选：阿里云 / 腾讯云静态托管

单独的静态托管只能发布前台静态资源，不能直接保留当前 CMS 写文件能力。若使用该方向，需要组合对象存储、云函数或云托管、数据库，并调整现有代码。相比之下，直接部署到腾讯云 CloudBase Run 更契合当前项目。

## 最终建议

1. 面向国内招聘且需要在线 CMS：腾讯云 CloudBase Run + CloudBase 数据库 + 云存储。
2. 需要最快上线、主要面向海外或跨地区招聘：Vercel + 托管数据库 + 对象存储。
3. 暂时只展示、不需要在线编辑：静态导出后可使用 GitHub Pages 或腾讯云静态托管。

当前项目若不修改 CMS 存储方式，最稳妥的是部署到带持久磁盘的常驻 Node.js 主机或容器；任何纯静态托管都无法完整承载现有功能。
